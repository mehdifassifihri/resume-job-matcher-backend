# Minimal requirements for Render deployment - NO RUST DEPENDENCIES
fastapi==0.104.1
uvicorn[standard]==0.24.0
openai==1.3.7
pydantic==2.5.0
langdetect==1.0.9
pypdf==3.17.4
python-docx==1.1.0
python-dotenv==1.0.0
python-multipart==0.0.6
# Use pure Python alternatives to avoid Rust compilation
PyJWT==2.8.0
# Remove sqlalchemy and alembic for now to avoid any potential issues
# sqlalchemy==2.0.23
# alembic==1.12.1
# Use pure Python hashing instead of bcrypt
# bcrypt==4.0.1
# Use pure Python cryptography alternatives
# cryptography==41.0.7
